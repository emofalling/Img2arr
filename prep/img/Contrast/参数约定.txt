[pack]struct{
    bool useint; //是否使用纯整数运算。如果是，性能将更高，但是画面精准度会下降。
    int16_t contrast; //对比度
    uint8_t centr;    //中心颜色R
    uint8_t centg;    //中心颜色G
    uint8_t centb;    //中心颜色B
    uint8_t centa;    //中心颜色A
    bool    opR;     //是否对R通道进行操作
    bool    opG;     //是否对G通道进行操作
    bool    opB;     //是否对B通道进行操作
    bool    opA;     //是否对A通道进行操作
}